[project]
name = "teleview"
version = "2.0"
description = "Simple, yet powerfull telegram (not only) parser"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13.3",
    "bs4>=0.0.2",
]
